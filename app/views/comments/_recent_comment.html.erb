<%= link_to "<i class='fa fa-circle text-info' aria-hidden='true' #{recent_comment.noticed?(current_user) ? 'hidden' : nil}></i> #{recent_comment.section.section_and_number}: #{recent_comment.user.full_name} #{time_ago_in_words(recent_comment.created_at)} ago".html_safe, section_comments_path(recent_comment.section), class: 'dropdown-item', data: {toggle: 'modal', target: '#comments', remote: true} %>